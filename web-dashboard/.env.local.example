# SSL Certificate Bypass for Development (NEVER use in production)
# Uncomment the line below if you're behind a corporate firewall/proxy
# NODE_TLS_REJECT_UNAUTHORIZED=0

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000
